# AI-MCP: Многофункциональный агент с поддержкой MCP и RAG

Этот проект представляет собой **интеллектуального агента**, интегрированного с протоколом [Model Context Protocol (MCP)](https://modelcontextprotocol.org/) и системой **поиска по знаниям (RAG)**. Агент может отвечать на вопросы, используя как локальные инструменты (через MCP), так и извлечённые документы из базы знаний.

---

## 🚀 Возможности

- **Интеграция с MCP**: запуск и выполнение локальных инструментов (например, Git, Docker и др.).
- **RAG (Retrieval-Augmented Generation)**: поиск по загруженным документам (PDF, текстовые файлы и т.п.) для улучшения качества ответов.
- **Поддержка LLM**: работает с `ollama`, `GigaChat` и другими через унифицированный интерфейс.
- **Модульная архитектура**: легко расширяемая структура для добавления новых инструментов и источников данных.
- **CLI-режим**: интерактивный режим для общения с агентом в терминале.

---

## 📦 Технологии

- **TypeScript** — основной язык
- **Node.js** — выполнение серверной логики
- **MCP SDK** — взаимодействие между клиентом и сервером инструментов
- **RAG** — поиск по векторным представлениям документов
- **Ollama / GigaChat** — модели ИИ
- **Winston** — логирование
- **Zod** — валидация схем
- **PDF-parse** — извлечение текста из PDF

---

## 🔧 Установка

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/your/repo.git ai-mcp
   cd ai-mcp
   ```

2. Установите зависимости:

   ```bash
   yarn install
   # или
   npm install
   ```

3. Создайте .env файл:

   ```env
   NODE_ENV=development
   LOG_LEVEL=debug

   # Для Ollama (по умолчанию)
   OLLAMA_MODEL=llama3

   # Для GigaChat (если используется)
   GIGACHAT_CLIENT_ID=your_client_id
   GIGACHAT_CLIENT_SECRET=tour_client_secret
   GIGACHAT_CLIENT_SCOPE=GIGACHAT_API_PERS
   GIGACHAT_MODEL_TALK=GigaChat
   GIGACHAT_MODEL_TOOLS=GigaChat
   ```

---

🛠️ Сборка и запуск

Собрать проект:

```bash
yarn build
```

Запустить в интерактивном режиме:

```bash
yarn dev --cli
```

Альтернативно:

```bash
yarn start --cli
```

🤖 Как это работает?

1. Пользователь вводит запрос, например: "Какие коммиты были вчера?"
2. RAG-система ищет релевантные документы (например, файлы изменений, документация).
3. Агент проверяет, нужно ли использовать локальные инструменты (например, git log).
4. Если нужно — вызывается MCP-инструмент, результат возвращается в контекст.
5. ИИ генерирует итоговый ответ, указывая источники:

**
Вчера были следующие коммиты: ...
Источники: [MCP: git_log], [RAG-источник 1]
**

---

## 🧩 Архитектура

**
src/
├── ai-agent/ # Логика агента и обработки чата
├── entrypoint/ # Точки входа (CLI и др.)
├── mcp/ # MCP-сервер и инструменты (Git и т.д.)
├── rag/ # RAG-модуль (поиск по документам)
└── index.ts # Главная точка входа
**

---

## 🔄 Добавление новых инструментов (MCP)

```ts
import { McpTool } from "./types";
import z from "zod";

export const DockerTools: McpTool<{ container: string }>[] = [
  [
    "docker_status",
    {
      title: "Статус контейнера",
      description: "Получить статус Docker-контейнера",
      inputSchema: z.object({ container: z.string() }),
    },
    async (req) => {
      // Логика через dockerode
      return { content: [{ type: "text", text: "running" }] };
    },
  ],
];
```

Затем добавьте в **src/mcp/index.ts**:

```ts
import { DockerTools } from "./dockerTools";
// ...
const TOOLS: McpTool<any>[] = [...GitTools, ...DockerTools];
```
