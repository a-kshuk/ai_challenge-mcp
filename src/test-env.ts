import { setupTestDB, closeTestDB } from "./db/test-db";

async function run() {
  try {
    await setupTestDB();
    // Можно добавить тестовые запросы для проверки
    console.log("🔥 Тестовая среда готова. БД заполнена моками.");
    while (true) {}
  } catch (error) {
    console.error("❌ Ошибка при запуске тестовой среды:", error);
    process.exit(1);
  } finally {
    // Не закрываем соединение сразу — пусть разработчик сам оценит состояние
    // await closeTestDB();
  }
}

run();
